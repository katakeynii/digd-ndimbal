<%= form_with(model: mairy) do |form| %>
  <% if mairy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mairy.errors.count, "error") %> prohibited this mairy from being saved:</h2>

      <ul>
        <% mairy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
